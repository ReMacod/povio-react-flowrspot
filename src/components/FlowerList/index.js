import React from 'react'
import classNames from 'classnames'

import './style.sass'

const className = classNames({
  FlowerList: true,
})

const FlowerList = () => <div className={className}>FlowerList</div>

export default FlowerList
